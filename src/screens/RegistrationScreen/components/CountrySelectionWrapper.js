import styled from 'styled-components';
import { scale } from 'react-native-size-matters';


export const CountrySelectionWrapper = styled.FlatList`
  flex: 1;
  marginTop: ${scale(20)};
  border-top-width: 1;
`;
